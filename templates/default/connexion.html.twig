{% extends 'base.html.twig' %}

{% block title %}Connexion
{% endblock %}

{% block body %}
	<header>
		<div id="titel">
			<h3>Connexion</h3>
		</div>
	</header>
	<div class="container">
		{{ form_start(form) }}
			<div class="my-custom-class-for-errors">
				{{ form_errors(form) }}
			</div>
			<div class="identifiant">{{ form_row(form.identifiant) }}</div>
			<div class="motdepasse"> {{ form_row(form.motdepasse) }} </div>
			<div class="zonedenvoie"> {{ form_row(form.connexion)}} </div>
			</div>
		{{ form_end(form) }}
	</div>
	{# <div>
		{{ connexion }} <br>
		{{ user }}
	</div> #}
{% endblock %}
