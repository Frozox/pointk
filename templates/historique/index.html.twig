{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/jquery.dataTables.min.css') }}" />
{% endblock %}

{% block body %}
    {{ include('navbar.html.twig') }}
    {{ include('loading.html.twig') }}
    <div class="container-fluid" style="display: none;">
        <div class="row">
            <div class="col-sm-1"></div>
            <div class="col-sm-10">
                <div class="tab-content">
                    {{ include('historique/list.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/jquery.dataTables.min.js') }}"></script>
    <script>
        //Variables twig mise en js pour implémentation dans historique.js
        //FIND
        var findCommandesToList = "{{ path('findcommandeshistorique') }}";
    </script>
    <script src="{{ asset('js/historique.js') }}"></script>
{% endblock %}