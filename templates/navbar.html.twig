<nav class="navbar sticky-top navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('accueil') }}"><img width="40px" style="vertical-align: inherit;" src="{{ asset('images/logo-site.png') }}">  Point K</a>
    <span class="navbar-text">
        <span class="solde-info">solde : <span class="user-solde-number">{{ app.user.solde }}</span> <img src="{{ asset('images/placeholder/chestnut.png') }}"></span>
        <div style="display: inline;" class="dropdown show ">
  <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    <i class="fas fa-user"></i>
  </a>

  <div class="dropdown-menu dropdown-menu-right"  aria-labelledby="dropdownMenuLink">
    <a class="dropdown-item" href="{{ path('accueil') }}">Accueil</a>
    {% if app.user.roles()[0] == 'ROLE_ADMIN' %}
      <a class="dropdown-item" href="{{ path('admin') }}">Administration</a>
    {% endif %}
    <a class="dropdown-item" href="{{ path('logout') }}">Déconnexion</a>
  </div>
</div>
    </span>
</nav>


<!--<button type="button" class="btn btn-danger"><a href="{{ path('logout') }}">Deconnexion</a></button>-->